import styled from "styled-components";

const S = {
    container: styled.div``,
    items: styled.div`
        display: flex;
        flex-wrap: wrap;
        width: 80%;
        align-items: center;
        justify-content: space-between;
        margin: 0 auto;
    `,
};

export default S;
